// Minimal entry for 404.html: only pulls in the compiled Tailwind styles.
// The full main.js assumes homepage elements exist (nav, forms, menu grid, etc.)
// and isn't safe to run on a page that doesn't have them.
import '../css/style.css'
